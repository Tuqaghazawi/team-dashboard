# Web framework
Django==6.0.6
asgiref==3.11.1
sqlparse==0.5.5
tzdata==2026.2

# AI layer
pydantic==2.13.4
openai==3.5.0
python-dotenv==1.2.3

# Document generation (MDC / planning slides, coordinator reports)
python-pptx==1.0.2
openpyxl==3.1.5

# Multi-agent MDC workflow (Session 5). Needed at runtime: the dashboard imports
# it to draft a recommendation and hold it at the physician sign-off gate.
langchain==1.4.0
langchain-core==1.6.1
langchain-openai==1.6.0
langgraph==1.2.11
# Persistence, so a paused MDC case survives a restart.
langgraph-checkpoint-sqlite==3.1.1

# Guideline ingestion (add_guideline)
pypdf==6.1.1

# Deployment (Render)
gunicorn==23.0.0
whitenoise==6.9.0
dj-database-url==3.0.0
psycopg[binary]==3.2.13
